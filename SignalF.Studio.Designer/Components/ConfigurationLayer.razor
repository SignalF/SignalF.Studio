@using Scotec.Blazor.Diagrams.Renderer
@inherits NodeLayer<ConfigurationLayerModel, SignalProcessorNodeModel, SignalProcessorLinkModel>

<div class="" style="height: 100%; width: 100%;">
    @foreach (var node in LayerModel.GetNodes())
    {
        <NodeRenderer @key="node" Model="node"/>
    }
    @foreach (var link in LayerModel.GetLinks())
    {
        <LinkRenderer @key="link" Model="link"/>
    }
</div>
